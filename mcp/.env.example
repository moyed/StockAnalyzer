# URL of the StockAnalyzer Laravel API (no trailing slash)
API_BASE_URL=http://localhost:8000/api

# Laravel Sanctum bearer token for a service account user
# Generate with: php artisan tinker → User::first()->createToken('mcp')->plainTextToken
API_TOKEN=your-sanctum-token-here
